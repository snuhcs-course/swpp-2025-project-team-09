# OCR Paragraph Parser

This project is a simple Python script that sends an image to the Naver CLOVA OCR API,  
parses the recognized text, and groups it into paragraphs using bounding box positions.
